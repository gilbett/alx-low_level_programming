#include <stdio.h>
/**
 * main -  This program prints a string using printf
 * Return:  It should return 0 if it runs successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
