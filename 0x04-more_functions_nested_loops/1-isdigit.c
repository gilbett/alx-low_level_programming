#include "main.h"

/**
 * _isdigit - This function checks for a digit
 * @c: This is a character
 * Return: Always (1) if is a digit, (0) otherwise
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
